# --- Core (web app) ---
fastapi[all]
uvicorn
sqlmodel
psycopg[binary]
python-dotenv
gradio
httpx

# --- Vision + ML (your actual stack: MediaPipe + scikit-learn) ---
mediapipe
opencv-contrib-python
numpy
pandas
joblib
# Pin scikit-learn to the SAME version you trained the .pkl with, so the
# saved model loads without version-mismatch warnings, e.g.:
#   scikit-learn==1.6.1
scikit-learn